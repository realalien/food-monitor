<h1>Listing product_detail_entries</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Value</th>
    <th>Type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @product_detail_entries.each do |product_detail_entry| %>
  <tr>
    <td><%= product_detail_entry.name %></td>
    <td><%= product_detail_entry.value %></td>
    <td><%= product_detail_entry.type %></td>
    <td><%= link_to 'Show', product_detail_entry %></td>
    <td><%= link_to 'Edit', edit_product_detail_entry_path(product_detail_entry) %></td>
    <td><%= link_to 'Destroy', product_detail_entry, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product detail entry', new_product_detail_entry_path %>
