<%= form_for(@product_detail_entry) do |f| %>
  <% if @product_detail_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_detail_entry.errors.count, "error") %> prohibited this product_detail_entry from being saved:</h2>

      <ul>
      <% @product_detail_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="field">
    <%= f.label :type %><br />
    <%= f.text_field :type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
